using UnityEngine;

namespace DataModel 
{
    public class Car : Vehicle
    {
        // Start is called before the first frame update
        void Start()
        {
            base.Init();
        }
    }
}

